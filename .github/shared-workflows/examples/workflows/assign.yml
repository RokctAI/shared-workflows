name: Auto Assign
on:
  pull_request:
    types: [opened]

jobs:
  assign:
    uses: RokctAI/shared-workflows/.github/workflows/universal-assign.yml@main
    permissions:
      contents: read
      pull-requests: write
    secrets: inherit


