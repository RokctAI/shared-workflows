name: Auto Release
on:
  push:
    branches: [main]
    paths: 
      - 'version.json'

jobs:
  release:
    permissions:
      contents: write
    uses: RokctAI/shared-workflows/.github/workflows/universal-release.yml@main
    secrets: inherit
