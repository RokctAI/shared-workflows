name: Welcome Bot

permissions:
  issues: write
  pull-requests: write
on:
  pull_request:
    types: [opened]
  issues:
    types: [opened]

jobs:
  welcome:
    uses: RokctAI/shared-workflows/.github/workflows/universal-welcome.yml@main
    secrets: inherit
