name: Auto Label

permissions:
  issues: write
on:
  issues:
    types: [opened, edited]

jobs:
  label:
    uses: RokctAI/shared-workflows/.github/workflows/universal-labeler.yml@main
    secrets: inherit
