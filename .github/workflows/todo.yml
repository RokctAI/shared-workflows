name: ToDo Bot

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions: write-all
on:
  push:
    branches: [main]

jobs:
  todo:
    uses: RokctAI/shared-workflows/.github/workflows/universal-todo.yml@fed18a76a3107bcd8293a24d3e949addfdd09e84 # main
    secrets: inherit
