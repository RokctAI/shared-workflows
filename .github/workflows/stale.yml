name: Stale

permissions:
  issues: write
  pull-requests: write
on:
  schedule:
    - cron: '30 1 * * *'

jobs:
  stale:
    uses: RokctAI/shared-workflows/.github/workflows/universal-stale.yml@main
    secrets: inherit
