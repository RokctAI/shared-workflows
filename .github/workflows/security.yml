name: Security Scan

permissions:
  contents: write
  issues: write
  workflows: write

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    uses: RokctAI/shared-workflows/.github/workflows/universal-security.yml@main
    secrets: inherit
