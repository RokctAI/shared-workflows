name: Security Scan

permissions: write-all

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    uses: RokctAI/shared-workflows/.github/workflows/universal-security.yml@main
    secrets: inherit
