name: Code Quality

permissions:
  contents: write
  pull-requests: write
  issues: write
  workflows: write

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    uses: RokctAI/shared-workflows/.github/workflows/universal-linter.yml@main
    secrets: inherit


