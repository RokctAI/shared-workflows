name: Code Quality

permissions:
  contents: write
  issues: write
  pull-requests: write
  workflows: write

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    uses: RokctAI/shared-workflows/.github/workflows/universal-linter.yml@main
    secrets: inherit


