name: Code Quality
on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    permissions:
      contents: write
      issues: write
    uses: RokctAI/shared-workflows/.github/workflows/universal-linter.yml@main
    secrets: inherit

