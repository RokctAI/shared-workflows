name: Auto Assign
on:
  pull_request:
    types: [opened]

jobs:
  assign:
    uses: RokctAI/shared-workflows/.github/workflows/universal-assign.yml@latest
    permissions:
      pull-requests: write
    secrets: inherit

