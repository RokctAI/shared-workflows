name: Auto Label
on:
  issues:
    types: [opened, edited]

jobs:
  label:
    uses: RokctAI/shared-workflows/.github/workflows/universal-labeler.yml@latest
    permissions:
      issues: write
    secrets: inherit

